# POM Audit - Maven Build Workflow for ensemble

name: Maven Build - ensemble

on:
  push:
    branches: [ "master", "main", "develop" ]
    paths:
      - 'ensemble/**'
      - 'ensemble/pom.xml'
  pull_request:
    branches: [ "master", "main", "develop" ]
    paths:
      - 'ensemble/**'
      - 'ensemble/pom.xml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build ensemble
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
        
    - name: Build ensemble
      run: mvn -B clean install -DskipTests -fn
      working-directory: ensemble
      env:
        MAVEN_OPTS: '-Xmx2048m -Xms1024m'
        
    - name: Run tests for ensemble
      run: mvn -B test -fn
      working-directory: ensemble
      continue-on-error: true
      env:
        MAVEN_OPTS: '-Xmx2048m -Xms1024m'
        
    - name: Generate dependency tree
      run: mvn dependency:tree -DoutputFile=dependency-tree.txt
      working-directory: ensemble
      continue-on-error: true
      
    - name: Upload dependency tree
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: dependency-tree-ensemble
        path: ensemble/dependency-tree.txt
        retention-days: 30