# POM Audit - Maven Build Workflow for ensemble-clustering

name: Maven Build - ensemble-clustering

on:
  push:
    branches: [ "master", "main", "develop" ]
    paths:
      - 'ensemble-clustering/**'
      - 'ensemble-clustering/pom.xml'
  pull_request:
    branches: [ "master", "main", "develop" ]
    paths:
      - 'ensemble-clustering/**'
      - 'ensemble-clustering/pom.xml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build ensemble-clustering
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
        
    - name: Build ensemble-clustering
      run: mvn -B clean install -DskipTests
      working-directory: ensemble-clustering
      env:
        MAVEN_OPTS: '-Xmx2048m -Xms1024m'
        
    - name: Run tests for ensemble-clustering
      run: mvn -B test
      working-directory: ensemble-clustering
      continue-on-error: false
      env:
        MAVEN_OPTS: '-Xmx2048m -Xms1024m'
        
    - name: Generate dependency tree
      run: mvn dependency:tree -DoutputFile=dependency-tree.txt
      working-directory: ensemble-clustering
      continue-on-error: true
      
    - name: Upload dependency tree
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: dependency-tree-ensemble-clustering
        path: ensemble-clustering/dependency-tree.txt
        retention-days: 30